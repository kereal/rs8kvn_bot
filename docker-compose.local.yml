# Docker Compose for local development

services:
    tgvpn_bot:
        build: .
        container_name: tgvpn_bot_local
        restart: unless-stopped
        volumes:
            - ./.env:/app/.env:ro
            - ./data:/app/data
        environment:
            - TZ=Europe/Moscow

# Docker Compose for local development

services:
    rs8kvn_bot:
        build: .
        container_name: rs8kvn_bot_local
        restart: unless-stopped
        volumes:
            - ./.env:/app/.env:ro
            - ./data:/app/data
        environment:
            - TZ=Europe/Moscow
